<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_minimize"
        android:icon="@drawable/ic_fullscreen_exit_white_24dp"
        android:title="Minimize"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_style_choose"
        android:title="@string/settings_style"
        app:showAsAction="never"/>
    <item
        android:id="@+id/action_is_last_evolution_only"
        android:checkable="true"
        android:checked="true"
        android:title="@string/settings_last_evolution_only"
        app:showAsAction="never"/>

</menu>